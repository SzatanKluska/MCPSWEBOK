{
  "name": "swebok-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server skeleton for the SWEBOK knowledge base (config-driven transport).",
  "main": "dist/index.js",
  "bin": {
    "swebok-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0-beta.2",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
