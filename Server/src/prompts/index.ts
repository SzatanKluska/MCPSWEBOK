import type { McpServer } from "@modelcontextprotocol/server";

/**
 * Registers MCP prompts on the server.
 *
 * TODO: add reusable prompt templates here (e.g. "explain SWEBOK topic").
 *
 * Intentionally empty in the skeleton.
 */
export function registerPrompts(_server: McpServer): void {
  // no prompts yet
}
