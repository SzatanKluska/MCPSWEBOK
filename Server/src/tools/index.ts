import type { McpServer } from "@modelcontextprotocol/server";

/**
 * Registers MCP tools on the server.
 *
 * TODO: add SWEBOK tools here, e.g.:
 *   server.registerTool("search", { description, inputSchema }, handler);
 *
 * Intentionally empty in the skeleton.
 */
export function registerTools(_server: McpServer): void {
  // no tools yet
}
