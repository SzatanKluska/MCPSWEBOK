# Byte-compiled / cache
__pycache__/
*.py[cod]

# Virtual environment
.venv/

# Pipeline outputs (regenerated by the pipeline)
KnowledgeBasePipeline/output/

# Generated knowledge base (handoff artifact: chunks + vectors)
# Leading slash anchors this to the repo root only, so it does NOT also ignore
# the Server's src/infrastructure/knowledgeBase/ source folder.
/KnowledgeBase/

# Model cache
.cache/

# Node / MCP server
Server/node_modules/
Server/dist/
